[pytest]
minversion = 6.0
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q --tb=short
markers =
    unit: unit tests (fast, mocked)
    integration: integration tests (require bcc + kernel)
    smoke: quick smoke tests
